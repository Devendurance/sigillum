import { ComingSoonPage } from "../coming-soon-page";

export default function PolicyConfigPage() {
  return (
    <ComingSoonPage
      description="This surface will hold policy controls after the dashboard shell and public-safe proof views are locked."
      eyebrow="Disabled preview"
      title="Policy Config"
    />
  );
}
