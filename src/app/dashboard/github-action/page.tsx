import { ComingSoonPage } from "../coming-soon-page";

export default function GithubActionPage() {
  return (
    <ComingSoonPage
      description="This surface will host Sigillum GitHub Action controls once the public proof board is fully stabilized."
      eyebrow="Disabled preview"
      title="Github Action"
    />
  );
}
